# 13 — إجراءات تحتاج يدك أنت (لإنجازها غدًا)

> نفّذتُ إصلاحات **P0 الأمنيّة في الكود** على الفرع `claude/website-visual-audit-kihjic`
> (لم تُنشَر — الموقع الحيّ لم يتأثّر). البنود أدناه **تتطلّبك أنت** لأنها تحتاج أسرارًا/قاعدة/قرارًا.
> رتّبتُها بالأولويّة. الأخطر أولًا.

---

## 🔴 أ) قبل دمج إصلاحات P0 للإنتاج — إلزامي وإلا يُقفَل الموقع الحيّ
إصلاح SEC-001 يجعل **المصادقة إلزامية في الإنتاج**، وإصلاح SEC-006 يجعل **`AUTH_SECRET` إلزاميًّا**.
فإن دُمِجت بلا التجهيز التالي، **سيتوقّف الموقع الحيّ عن العمل** (لا أحد يدخل / يفشل الإقلاع).

- [ ] **١. ولّد سرّ الجلسة واضبطه في Vercel.**
  ```bash
  openssl rand -base64 32
  ```
  Vercel → Project → Settings → Environment Variables → أضِف `AUTH_SECRET` = الناتج (لكل من Production و Preview).
- [ ] **٢. أنشئ حساب مستخدم حقيقيًّا** (لأن دخول الزائر لن يعمل في الإنتاج):
  اضبط `INITIAL_ADMIN_EMAIL` و`INITIAL_ADMIN_PASSWORD` في Vercel ثم شغّل `db:seed`، **أو** أنشئ مستخدمًا عبر لوحة الأدمن قبل قفل الوصول.
- [ ] **٣. اضبط `DISABLE_AUTH=false`** (أو احذفه) في Vercel.
- [ ] **٤. جرّب تسجيل الدخول على رابط المعاينة (Preview)** للفرع قبل الدمج للإنتاج — تأكّد أنك تدخل بحسابك.
- [ ] **٥. بعد نجاح المعاينة فقط** → ادمج الفرع للإنتاج.

> بدون (١) و(٢): لا تدمج. أخبرني حين تجهّزها وأتحقّق معك من المعاينة.

---

## 🟠 ب) تطبيق قيد «نسخة نافذة واحدة» (VERSION-001) على Neon
- [ ] **٦.** شغّل على Neon (قراءة فقط): `audit/current-version-conflicts.sql` — إن ظهر أي `multiple_current` نظّفه أولًا.
- [ ] **٧.** طبّق المهاجرة `prisma/migrations/20260711120000_article_version_one_current` عبر **workflow مُقفَل** على Neon
  (نفس آليّة `legal-db-architecture`). ستفشل لو بقي تعارض — وهذا مقصود.

---

## 🟡 ج) شغّل استعلامات التدقيق (قراءة فقط) — تُكمل القرار بأرقام حقيقيّة
- [ ] **٨.** على Neon، شغّل ملفّات `audit/*.sql` واحفظ النتائج (خصوصًا: `data-inventory`, `orphan-records`,
  `duplicate-records`, `content-anomalies`, `source-validation`). أرسلها لي لأحوّل بنود «تعذّر فحصه» إلى نتائج حاسمة.
  ```bash
  psql "$DATABASE_URL" -f audit/data-inventory.sql
  # ... بقيّة الملفّات
  ```

---

## 🔵 د) قرارات (سياسة) — جوابك يحدّد الخطوة
- [ ] **٩. مزوّد الذكاء (SEC-008):** هل نلتزم **Claude حصريًّا** (نُعطّل OpenAI/Gemini)، أم نُبقيها مع **أساس PDPL موثّق**؟
- [ ] **١٠. عنقود OpenSearch:** (لا يزال معلّقًا) — أنشئ Bonsai Standard Fir وأضِف الأسرار، وأنا أفهرس.

---

## ما أنجزتُه أنا بالفعل (لا يحتاجك — جاهز على الفرع)
- ✅ فرض المصادقة افتراضيًّا + منعها إطلاقًا في الإنتاج (SEC-001).
- ✅ إغلاق ثغرات IDOR: قضية/قائمة قضايا/محاكاة/مرفقات — فحص ملكيّة على كلٍّ (SEC-002..005).
- ✅ إبطال سرّ الجلسة المكتوب: فشل إلزاميّ بلا `AUTH_SECRET` في الإنتاج (SEC-006).
- ✅ تعمية PDPL مركزيّة في بوّابة الذكاء لمسار الاستشارة/القاضي (SEC-007).
- ✅ ملفّ مهاجرة قيد «النسخة النافذة الواحدة» (جاهز للتطبيق عبر workflow).
- ✅ `.env.example` محدّث بالإعدادات الآمنة.
