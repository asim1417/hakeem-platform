# كتالوج خدمات منصة حكيم

**المرجع:** `main@56c7a0e` — 31 يوليو 2026م  
**مفتاح الحالة:** [مثبت] · [جزئي] · [واجهة فقط] · [خلفية فقط] · [تجريبي] · [غير متحقق]

## 1. اسأل حكيم

| البند | البيان |
|---|---|
| المستفيد | المحامي، المستشار، الباحث، الطالب |
| المدخلات | سؤال، وقائع، نص مستند، وضع إخراج |
| المخرجات | جواب، تحليل قضية، خطة عمل، تقدير تدريبي، مصادر وتحذيرات |
| التشغيل | مصادقة → بوابة استخدام → مهمة قابلة للاستئناف → وكيل أصيل أو منسق → استرجاع/تحقق → بث وحفظ |
| المعرفة | النواة القانونية، المرفق، تاريخ الجلسة |
| الصلاحية | `LEGAL_CORE_VIEW` وتسجيل الدخول |
| الحالة | **[مثبت] مكتملة ومتصلة بنيويًا**؛ النموذج وقاعدة الإنتاج [غير متحقق] |
| المسارات | `/dashboard/ask`، `/api/ai/agent-search` |
| الأدلة | `app/api/ai/agent-search/route.ts`، `components/ask/*`، `lib/modules/hakeem-agent/*` |
| القيود | جودة النتيجة مرتبطة بكفاية الوقائع والمصادر؛ لا تعد رأيًا نهائيًا |

## 2. محرك الجلسات

| البند | البيان |
|---|---|
| المستفيد | جميع مستخدمي المحادثة والمعاون |
| المدخلات | رسائل، مرفقات، حالة، مصادر، نموذج، أدوات |
| المخرجات | جلسة دائمة قابلة للبحث والتثبيت والأرشفة والتفرع |
| الحالة | **[مثبت] مكتملة ومتصلة** |
| المسارات | `/api/conversations`، `/api/conversations/[id]`، `/messages` |
| قاعدة البيانات | `ChatConversation`, `ChatMessage` |
| الأدلة | `lib/modules/conversations/*`، `prisma/schema.prisma` |

## 3. النواة القانونية الموحدة

| البند | البيان |
|---|---|
| المستفيد | المستخدم، المشرف، جميع خدمات الذكاء |
| المدخلات | بحث، نظام، مادة، تصنيف، مرجع |
| المخرجات | مواد، أنظمة، أحكام، مبادئ، علاقات، تغطية وجودة |
| الحالة | **[مثبت] مكتملة بنيويًا**؛ المحتوى الحي والإحصاء [غير متحقق] |
| المسارات | `/dashboard/legal-core/*`، `/api/legal-core/*`، `/api/legal/*` |
| قاعدة البيانات | `LegalSystem`, `LegalArticle`, `ArticleAmendment`, `ArticleVersion`, `JudicialCase`, `JudicialPrinciple` |
| الأدلة | `app/dashboard/legal-core/page.tsx`، `lib/modules/legal-core/*` |
| القيود | الشروح والقانون المقارن والقوالب موسومة مرحلية في الواجهة |

## 4. البحث القانوني النصي والهجين

| البند | البيان |
|---|---|
| المدخلات | عبارة وفلاتر ونظام وموضوع |
| المخرجات | نتائج مرتبة ومقاطع وروابط داخلية |
| المصادر | PostgreSQL full-text، BM25، pgvector عند تعبئة المتجهات، rerank |
| الحالة | **[مثبت] عاملة بنيويًا**؛ تغطية المتجهات [غير متحقق] |
| المسارات | `/api/legal-search`، `/api/legal-core/search`، `/bm25-search`، `/api/embeddings/status` |
| الأدلة | `lib/modules/legal-core/legal-retrieval.ts`، `lib/modules/legal-search/*` |

## 5. RAG والاستشهادات

| البند | البيان |
|---|---|
| المخرجات | إجابة مؤسّسة على مواد مسترجعة مع استشهادات |
| الحوكمة | حجب عند غياب المادة، فحص أرقام المواد، تسجيل Guardrail |
| الحالة | **[مثبت]** |
| المسارات | `/api/legal-rag`، `/dashboard/legal-rag`، `/api/legal-core/citations/analyze` |
| الأدلة | `lib/modules/legal-rag/*`، `lib/modules/citations/*`، `lib/modules/ai/ai-gateway.ts` |

## 6. تحليل القضية

| البند | البيان |
|---|---|
| المدخلات | وقائع ونوع نزاع ومرحلة |
| المخرجات | مسائل، دفوع، مواد، تقدير قوة أو نواقص بحسب المسار |
| الحالة | **[مثبت] خدمة قائمة**، مع تداخل وظيفي مع Ask والمعاون |
| المسارات | `/dashboard/case-analysis`، `/api/case-analysis` |
| الأدلة | `lib/modules/case-analysis/*` |
| القيود | لا يحفظ كل تشغيل في نموذج قضية موحد في جميع المسارات القديمة |

## 7. خطة العمل القانونية

| البند | البيان |
|---|---|
| المدخلات | تحليل أو وقائع |
| المخرجات | إجراءات وخطوات وأولويات |
| الحالة | **[مثبت]** مع احتمال دمجها داخل Ask |
| المسارات | `/dashboard/legal-agent`، `/api/legal-agent` |
| الأدلة | `lib/modules/legal-agent/*` |

## 8. الاستشارات القانونية

| البند | البيان |
|---|---|
| المدخلات | الوقائع |
| المخرجات | مسودة استشارة واستشهادات وسجل |
| الحالة | **[مثبت] متصلة بقاعدة البيانات** |
| المسارات | `/dashboard/consultations`، `/api/ai/consultation` |
| قاعدة البيانات | `Consultation`, `ConsultationCitation`, `Message` |
| الأدلة | `app/api/ai/consultation/route.ts`، `lib/modules/consultations/*` |

## 9. المحادثة القانونية

| البند | البيان |
|---|---|
| المدخلات | حوار وسياق قضية |
| المخرجات | محادثة قانونية محفوظة |
| الحالة | **[مثبت]**، لكنها سطح موازٍ لـ Ask الحديث |
| المسارات | `/dashboard/legal-chat`، `/api/legal-chat` |
| الأدلة | `lib/modules/legal-chat/*` |

## 10. المعاون القضائي

| البند | البيان |
|---|---|
| المستفيد | القاضي أو الممارس الذي يدير قضية داخلية |
| المدخلات | قضية، مرفقات، خريطة، مرحلة، سؤال أو عمل قضائي |
| المخرجات | 24 نوع عمل قضائي، سجل تحليلات، تصدير |
| الحالة | **[مثبت] متقدمة**؛ الخدمات النموذجية [غير متحقق] حيًا |
| المسارات | `/dashboard/judicial-assistant/*`، `/api/judicial-assistant/*` |
| قاعدة البيانات | `JudicialWorkCase`, `JudicialAnalysis`, `ChatConversation` |
| الأدلة | `lib/modules/judicial-assistant/*`، `scripts/verify-judicial-assistant.ts` |
| القيود | ليس موصل تقاضٍ ولا جهة قضائية؛ المراجعة البشرية إلزامية في أغلب الأعمال |

### كتالوج المعاون: 24 خدمة

| الرمز | الخدمة | نمط المراجعة |
|---|---|---|
| JS-001 | الملخص التنفيذي | اختيارية |
| JS-002 | مذكرة الإحاطة | مطلوبة |
| JS-003 | الملخص التفصيلي | مطلوبة |
| JS-004 | الخط الزمني | مطلوبة |
| JS-005 | خريطة القضية | مطلوبة |
| JS-006 | فحص الاختصاص | إلزامية |
| JS-007 | فحص القبول | إلزامية |
| JS-008 | تحليل الإجراءات | إلزامية |
| JS-009 | حساب المدد | إلزامية |
| JS-010 | مصفوفة الإثبات | إلزامية |
| JS-011 | تحضير جلسة | مطلوبة |
| JS-012 | مقارنة الأقوال | مطلوبة |
| JS-013 | الدراسة القضائية | إلزامية |
| JS-014 | مذكرة مسألة | إلزامية |
| JS-015 | قرار إجرائي | إلزامية |
| JS-016 | صياغة الوقائع | إلزامية |
| JS-017 | بناء التسبيب | إلزامية |
| JS-018 | مشروع الحكم | إلزامية |
| JS-019 | فحص المنطوق | إلزامية |
| JS-020 | فحص جودة الحكم | إلزامية |
| JS-021 | تحليل الاعتراض | إلزامية |
| JS-022 | مذكرة الرد على الاعتراض | إلزامية |
| JS-023 | التصدير القضائي | اختيارية |
| JS-024 | قائمة عمل القاضي | اختيارية |

## 11. القاضي التفاعلي

| البند | البيان |
|---|---|
| المستفيد | التدريب والممارسة الداخلية |
| المدخلات | دعوى، أطراف، مداخلات، أدلة، قرارات |
| المخرجات | جلسة، أدوار، قرارات، حكم تدريبي، اعتراض، صلح، محضر، تصدير |
| الحالة | **[مثبت] عاملة ومخزنة** |
| المسارات | `/dashboard/simulations`، `/api/simulations/*` |
| قاعدة البيانات | `Simulation`, `SimulationMessage`, `SimulationDecision`, `SimulationJudgment` |
| الأدلة | `lib/modules/simulations/*`، `app/api/simulations/[id]/messages/route.ts` |
| القيود | محاكاة تعليمية؛ لا توقع قانوني ملزم ولا حكم حقيقي |

## 12. منصة الوثائق — محطة العمل

| البند | البيان |
|---|---|
| المدخلات | PDF، DOCX، TXT، JSON، صور، نص منسوخ |
| المخرجات | نص، تصنيف، رمز، كيانات، جداول، مقتطفات، تصدير |
| التشغيل | غالب الفحص في المتصفح؛ حفظ القضية اختياري؛ خدمة خادم اختيارية |
| الحالة | **[مثبت] منتج قائم** |
| المسارات | `/documents`، `/documents/app`، `/documents/tool` |
| الأدلة | `app/documents/*`، `lib/modules/document-inspection/*` |
| القيود | ادعاءات السرعة والدقة تحتاج قياسًا؛ OCR السحابي يحتاج مفتاحًا |

## 13. المرفقات العامة

| البند | البيان |
|---|---|
| المدخلات | PDF/DOCX/TXT/PNG/JPG أو URL |
| المخرجات | ملف وبيانات وصفية وحالة معالجة |
| الحالة | **[جزئي]** |
| المسارات | `/api/attachments`، `/import-url`، `/inspect-url`، `/download` |
| الأدلة | `app/api/attachments/route.ts`، `lib/modules/attachments/*` |
| القيود | الرفع العام لا يستخرج النص مباشرة؛ `extractedText` يبقى null في المسار المفحوص |

## 14. استيراد Google Drive

| البند | البيان |
|---|---|
| الحالة | **[مثبت بنيويًا/غير متحقق بيئيًا]** |
| المسارات | `/api/doc-platform/drive/*` |
| الأدلة | `lib/modules/doc-platform/google-drive.ts` |
| القيود | يحتاج OAuth ومفاتيح صالحة؛ لا دليل على OneDrive/Dropbox/Box مكتمل رغم وجود قيم مصدر في المخطط |

## 15. إدارة القضايا والملفات

| البند | البيان |
|---|---|
| المخرجات | ملفات قضايا، مرفقات، مجلدات، تظليلات، مساحة المستخدم |
| الحالة | **[مثبت]** |
| المسارات | `/dashboard/cases`، `/dashboard/files`، `/api/cases`، `/api/folders`، `/api/annotations` |
| الأدلة | `CaseFile`, `Attachment`, `Folder`, `Annotation` في Prisma |

## 16. التدريب القانوني

| البند | البيان |
|---|---|
| الحالة | **[جزئي]** |
| المسارات | `/dashboard/training`، `/api/training/attempts` |
| قاعدة البيانات | `Exercise`, `TrainingProgress` |
| القيود | اكتمال المناهج وعدد التمارين في الإنتاج غير متحقق |

## 17. الحساب والحصة والنقاط

| البند | البيان |
|---|---|
| المخرجات | رصيد، حصة، استهلاك، مكافآت، منع عند النفاد |
| الحالة | **[مثبت]** |
| المسارات | `/api/credits*`، `/api/billing/status`، `/dashboard/billing` |
| الأدلة | `lib/modules/billing/*`، `lib/modules/credits/*` |

## 18. الاشتراكات والدفع

| البند | البيان |
|---|---|
| الخطط | مجانية، محترف، فريق |
| المزود | Moyasar عند وجود المفتاح |
| الحالة | **[جزئي/مشروط بالبيئة]** |
| المسارات | `/pricing`، `/dashboard/subscribe`، `/api/billing/checkout`، `/webhook` |
| الأدلة | `config/pricing.ts`، `app/api/billing/*` |
| القيود | حالة الدفع الحي والفوترة النهائية غير متحققة |

## 19. الإدارة والحوكمة

| البند | البيان |
|---|---|
| الوظائف | مستخدمون، أدوار، إعدادات، ذكاء، مفاتيح API، موقع، وظائف، استخدام، فوترة، دعم، تدقيق |
| الحالة | **[مثبت]** |
| المسارات | `/admin/*`، `/api/admin/*`، `/audit-logs` |
| الأدلة | `app/admin/*`، `lib/modules/auth/rbac.ts`، `lib/modules/audit/audit.ts` |

## 20. بوابة API للمطورين

| البند | البيان |
|---|---|
| المخرجات | بحث وأنظمة ومواد وعلاقات وفق نطاق المفتاح |
| الحماية | SHA-256 للمفتاح، scopes، rate limit، CORS |
| الحالة | **[مثبت] بنيويًا** |
| المسارات | `/developers`، `/api-docs`، `/api/openapi`، `/api/legal/*` |
| قاعدة البيانات | `ApiKey`, `ApiRequestWindow` |

## 21. البحث في التراث والطبقة الفقهية

| البند | البيان |
|---|---|
| الحالة | **[جزئي]** |
| المسارات | `/api/turath/search`، `/api/legal/articles/[id]/fiqh` |
| قاعدة البيانات | `FiqhSource`, `FiqhText`, `FiqhIssue`, `FiqhArticleAlignment` |
| القيود | المواءمات تحتاج مراجعة بشرية ولا تُعرض بوصفها حكمًا نظاميًا |

## 22. الإشعارات والنسخ الاحتياطية

- **الإشعارات:** **[غير مثبت]** لم يظهر كيان أو خدمة إشعار مكتملة في الأدلة المفحوصة.
- **النسخ الاحتياطية الخادمية:** **[غير متحقق]** لا توجد في المستودع سياسة تشغيل موثقة كافية؛ النسخة المشفرة المحلية للوثائق ليست بديلًا عن Backup للبنية وقاعدة البيانات.

## 23. خدمات لا يجوز تسويقها كقدرات مكتملة دون تحقق

- OneDrive وSharePoint وDropbox وBox لمجرد وجود قيم enum أو حقول مصدر.
- الدفع الحي لمجرد وجود مسارات Moyasar.
- الامتثال الكامل لـPDPL.
- اكتمال جميع الأنظمة والأحكام والمبادئ.
- دقة التوقع القضائي أو ضمان منع التلفيق.
