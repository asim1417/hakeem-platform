# فحص القواعد المترابطة (قراءة-فقط)
## connected_databases_inspection.md

> التاريخ: 2026-06-21 · عبر GitHub Actions (قراءة-فقط، لا كتابة). **لم تُطبع أي أسرار.**

## النتيجة الحاسمة
قاعدة الإنتاج التي يشير إليها سرّ **`DATABASE_URL`** (هدف workflow الترحيل) — فُحصت عبر `db-inspect` (نجح):

| الجدول | عدد الصفوف (الإنتاج) |
|---|---|
| **judicial_cases (الأحكام)** | **51,105** ✅ |
| legal_articles (المواد) | **15,902** ✅ |
| legal_systems (الأنظمة) | **489** ✅ |
| legal_article_case_links (روابط مادة↔حكم) | **29,705** ✅ |
| judicial_principles (المبادئ) | 0 ⚠️ |
| users | 4 |
| + جداول legal_thesaurus_* كاملة | موجودة ✅ |

## الخلاصة
1. **الأحكام ليست مفقودة في الإنتاج** — القاعدة الإنتاجية فيها **51,105 حكمًا** و**15,902 مادة** و**29,705 رابط** مادة↔حكم. «الفراغ» الذي ظهر سابقًا كان في **قاعدة التطوير المحلية فقط** (بذر مختصر: 9 أنظمة/1981 مادة/6 أحكام تجريبية).
2. **التدقيق القديم متجاوَز:** كان يقول B فيها 0 حكم/1981 مادة — الآن 51k حكم/15.9k مادة. أي استيراد الأحكام **تمّ فعلًا** (عبر مسار سابق)، فلا حاجة لإعادة تشغيل `migrate-judgments`.
3. **الثغرة الوحيدة:** `judicial_principles = 0` (لم تُستخرج المبادئ بعد) — ثانوية.

## التوصيات
| # | التوصية | النوع |
|---|---|---|
| 1 | **عدم** تشغيل `migrate-judgments` — البيانات موجودة (سيكون تكرارًا) | تجنّب كتابة لا لزوم لها |
| 2 | تأكيد أن سرّ `DATABASE_URL` في **Vercel (التشغيل)** = نفس هذه القاعدة | تحقّق تشغيلي |
| 3 | لتفعيل تحسينات البحث الجديدة على الإنتاج: تطبيق كياني `LegalTopic`/`SearchLog` + فهارس trigram (workflow «Neon Retrieval Upgrade» أو `db push`) | كتابة (باختيارك) |
| 4 | استخراج المبادئ القضائية (judicial_principles=0) لاحقًا | إثراء |

## ملاحظة على عملي في هذه الجلسة
كل تطويري (البحث الشامل، الهوية، الكيانات، الفهارس) **يعمل أفضل على الإنتاج** لأن فيه بيانات أضخم بكثير (51k حكم/15.9k مادة مقابل 1981 مادة محليًا). يتبقّى فقط تطبيق إضافاتي على قاعدة الإنتاج عند الدمج.
