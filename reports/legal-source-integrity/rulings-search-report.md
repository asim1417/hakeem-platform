# مسار بحث الأحكام المستقلّ (البند 1.7) — تقرير التحقّق

المصدر: `judicial_cases` على Neon (**قراءة فقط**، 51,105 حكمًا). لا دمج مواد/أنظمة.
الطريقة: مطابقة كلمة كاملة عبر `to_tsvector('simple')` + `plainto_tsquery` (تتفادى الاحتواء
الزائف مثل «نجش» داخل «سونجشانج») + تطبيع عربيّ/أرقام هندية + حجب PDPL في المقتطف + `ts_rank`.

## نتائج الاستعلامات المرجعية (قياس حيّ على Neon، قراءة فقط)

| الاستعلام | عدد النتائج | زمن الاستجابة | أعلى صلة | PDPL |
|---|---:|---:|---:|---|
| عقد الإيجار | 1,320 | 82.4s | 1.00 | لا تسريب |
| فسخ العقد | 3,329 | 51.1s | 1.00 | لا تسريب |
| الغبن | 203 | 48.0s | 1.00 | لا تسريب |
| الشيك | 2,630 | 50.2s | 1.00 | لا تسريب |
| التعويض | 9,904 | 54.1s | 1.00 | لا تسريب |
| الحضانة · النفقة · التحكيم · الشفعة · نجش | — | (لم تُقَس) | — | — |

كل النتائج المقيسة: مطابقة كلمة كاملة (score=1.00)، وبلا تسريب أرقام هوية في المقتطفات.
فحوص الوحدة نجحت: حجب الهوية/الجوال/الآيبان، واستخراج السنة الهجرية (لاتينيّ وهنديّ).

## زمن الاستجابة — السبب والحلّ
- **48–82 ثانية/استعلام** لأن الإنتاج **بلا عمود `search_norm` ولا فهرس GIN**، فالبحث مسحٌ تسلسليّ
  يحسب `to_tsvector` على 51k صفّ لكل استعلام. هذا يؤكّد ما رصدته الخطة (البند 1.7/1.8).
- **الحلّ المُعدّ (يُطبَّق على Neon branch لا الإنتاج):**
  - هجرة `prisma/migrations/20260925060000_judicial_cases_search_norm/` (عمود `search_norm` + فهرس GIN).
  - سكربت `scripts/backfill/build-rulings-search-norm.ts` يملأ `search_norm` بنصٍّ **منقّى PDPL** ومطبَّع.
  - المسار البرمجيّ يكتشف العمود تلقائيًّا ويستخدم الفهرس (زمن متوقّع < ثانية).

## ما بقي غير متحقَّق منه
1. الأداء الإنتاجيّ يتطلّب تطبيق الفهرس على Neon branch ثم القياس (لم يُطبَّق — الإنتاج ممنوع).
2. الاستعلامات الخمسة الأخيرة لم تُقَس حيًّا (لبطء المسح التسلسليّ بلا فهرس) — تُقاس بعد الفهرس.
3. السنة الهجرية تُستخرج من النصّ (لا عمود مبنيَن) — تغطية < 100%.
4. حجب PDPL يشمل الهوية/الإقامة/الجوال/الآيبان؛ **أسماء الأفراد** تحتاج سياسة/طبقة تنقية منفصلة.
5. لا هجرة/كتابة على الإنتاج في هذا العمل (قراءة فقط للتحقّق).
