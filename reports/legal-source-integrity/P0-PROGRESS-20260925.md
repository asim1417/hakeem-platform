# تنفيذ بنود P0 من خطة الإصلاح (التقرير الثاني عشر) — 2026-09-25

فرع: `cursor/p0-legal-guard-dedupe-28d4` (من main بعد دمج #655). (البند 1.7 في PR منفصل #657.)

## 1.1 — حلّ اسم النظام في الحارس والمحرك ✅ (مُنفَّذ ومُختبَر، بعد التصحيح)
- `lib/modules/legal-core/resolve-law.ts` (جديد): `normalizeSystemName`، `isBylawName`، `isNumberingShifted`، و`resolveLaw`.
- **الاحتواء لم يعد اختيارًا حاسمًا**: `resolveLaw` يعيد `decisive=true` فقط للمطابقة التامّة الوحيدة أو أطول
  بادئة وحيدة؛ وإلّا `decisive=false` مع `candidates` (اقتراح لا اختيار).
- الحارس `validateLegalCitation`: عند عدم الحسم → `CITATION_NOT_VERIFIED` مع المرشّحين.
- **UNKNOWN لا يُعامَل نافذًا**: `inForce=true` فقط عندما `status="سارية"` صراحةً؛ والملغاة `repealed=true`.
- **أنظمة إزاحة الترقيم (1.4)**: أيّ استشهاد بمادة في نظامٍ ضمن `data/shifted-systems.json.pendingOfficialCorrection`
  يعيد `CITATION_NOT_VERIFIED` حتى التصحيح الرسميّ (المعاملات المدنية مُصحَّح فمُستبعَد).
- MCP `verifyCitation`: يستخدم نفس المنطق (لا حسم بالاحتواء + رفض المزاح + حكم «ملغاة»).
- اختبار `npm run test:citation-guard` — **14/14**.

## 1.5 — المواد المكررة داخل النظام — «كشف مكتمل، معالجة معلقة»
- `scripts/audit/detect-duplicate-articles.ts` (قراءة فقط): كشف النصوص المتطابقة داخل النظام.
- نتيجة على Neon: **7 أنظمة، 11 مجموعة، 11 صفًّا زائدًا**.
- **فرق 11/12:** ذكرت الخطة 12 مجموعة؛ الكشف الحيّ الحاليّ يجد 11. السبب: (أ) عتبة تجاهل النصوص القصيرة
  (<25 حرفًا) هنا لتقليل الضجيج قد تستبعد مجموعة قصيرة، و(ب) بعض التكرارات السابقة عولجت (تكرار مادة النفاذ
  في المعاملات المدنية أُصلح سابقًا). التسوية مُدرجة في `duplicate-articles.json`.
- **المعالجة معلّقة**: الأرشفة (`archived_at` + `archive_reason`، بلا حذف) تحتاج مطابقة المصدر ومراجعة بشرية (يدويّ).

## 1.8 — إعادة بناء BM25 — «تهيئة موجودة، التنفيذ النهائي مؤجل لما بعد إصلاح البيانات»
- التهيئة موجودة: `npm run build:bm25:db` / `refresh:indexes` (بناء من القاعدة `--from-db`).
- **التنفيذ النهائي مؤجَّل** لما بعد اكتمال إصلاحات البيانات (خرائط الأرقام لبقية الأنظمة، الأرشفة)، ويُشغَّل
  في CI/تشغيل مضبوط لا كالتزام ملف ثنائي هنا.

## المتبقّي من P0 (يحتاج مصدرًا/مراجعة)
- 1.2 (الأزواج الخمسة SUPERSEDES)، 1.4 (خرائط أرقام بقية الأنظمة — تحتاج هيئة الخبراء)،
  1.6 (إعادة بناء نظام العمل)، 1.7 (منفصل: PR #657).
