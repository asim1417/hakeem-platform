# أمر تشغيل — إدخال ديباجات الأنظمة (صيغ الإصدار)

**إلى:** وكيل برمجيّ متخصّص · **الفرع:** `aman/legal-db-update` · **PR:** #631
**من:** جلسة تدقيق قاعدة الأنظمة — ٢٠٢٦-٠٩-٢٥
**الملف المرافق:** `data/backfill/text/civil-transactions-law-preamble.json`

---

## ١) المشكلة

**ديباجات الأنظمة غائبة من المدونة** — أي كل النصّ الذي يسبق المادة الأولى:
البسملة · أداة الإصدار (المرسوم الملكي) · الافتتاحية («نحن سلمان بن عبدالعزيز…»)
· الاستنادات («بناءً على…» / «بعد الاطلاع على…») · قرار مجلس الوزراء · **بنود
المنطوق** (أولًا، ثانيًا، ثالثًا…).

### الفحص المُثبِت

| الفحص | النتيجة |
|---|---|
| مستندات برقم مادة = ٠ في فهرس الاسترجاع | **صفر** |
| أنظمة تبدأ من المادة ١ (أي بلا ديباجة) | **٤٨٥ من ٤٨٥** |
| مواد نصّها يبدأ بصيغة مرسوم | **صفر** |
| ملفات ديباجات في المستودع | **١** |

وتأكّد حيًّا: `نظام التوثيق` يبدأ فهرسه من المادة ١ مباشرة.

### لماذا هذا حرج قانونيًّا

1. **لا يمكن إثبات سريان نظام** بلا أداة إصداره وتاريخ نشره.
2. **بنود الإلغاء والتعديل تعيش في المنطوق لا في المواد.** فكل ما استُخرج سابقًا
   من «١٦٤ مادة تحمل لغة إحلال» جاء من المواد الختامية فقط؛ أمّا إلغاءات
   المراسيم فهي **خارج المدونة كليًّا**. وهذا يفسّر جانبًا من مشكلة «الملغى
   يُعرض كساريّ».
3. **الأحكام الانتقالية** لا تظهر في أي مادة — ومنها في نظام المعاملات المدنية
   حكمٌ حاسم بالأثر الرجعي المشروط.
4. لا يستطيع المحامي صياغة إسناد رسميّ من بيانات المنصّة.

---

## ٢) المُسلَّم في هذه الحزمة

`data/backfill/text/civil-transactions-law-preamble.json` — ديباجة **نظام واحد**
(المعاملات المدنية)، وهي **الوحيدة المجموعة رسميًّا والمتوفّرة في المستودع**.

| المفتاح | المحتوى |
|---|---|
| `ingest_payload` | الشكل الذي يقبله `scripts/backfill/ingest.ts` مباشرة (`docType`/`rawText`/`number`/`hijriDate`/`sourceUrl`) |
| `system` | البطاقة الموحّدة: المرسوم، قرار المجلس، قرارا الشورى، ٥ استنادات، ٧ بنود منطوق |
| `legal_effects` | **٨ آثار نظامية مستخرجة حرفيًّا** |
| `_meta` | بصمتا `raw_sha256` و`normalized_sha256` وحالة التحقق |

### الآثار الثمانية — لم تكن في المدونة قبل هذا

| # | النوع | النظام الهدف | الموضع |
|---|---|---|---|
| ١ | إلغاء | المرافعات الشرعية | المواد ٢١٢–٢١٧ |
| ٢ | إلغاء جزئي | المرافعات الشرعية | عبارة في عجز م٢١١ |
| ٣ | إلغاء | المحاماة | م٢٥ |
| ٤ | إلغاء جزئي | أخلاقيات البحث على المخلوقات الحية | تعريفات في م١ |
| ٥ | تعديل بالإحلال | المحاكم التجارية | م١٦/٣ |
| ٦ | تعديل بالإحلال | ضمان الحقوق بالأموال المنقولة | تعريف في م١ |
| ٧ | تعديل بالإحلال | التوثيق | م١٥/٨ |
| ٨ | حكم انتقالي | المعاملات المدنية | أثر رجعي مشروط |

> ⚠️ الأثران ١ و٢ يمسّان **نظام المرافعات الشرعية** — وهو من الأنظمة التسعة
> الأساسية. تحقّق أن مواده ٢١١–٢١٧ ما تزال معروضة «سارية» في المدونة.

---

## ٣) المطلوب — بالترتيب

### المرحلة (أ) — إدخال الديباجة المتوفّرة

```bash
git checkout aman/legal-db-update && npm install

# معاينة (لا كتابة)
npx tsx scripts/backfill/ingest.ts

# الإدخال — مقفول خلف بوابتين
CONFIRM_RUNTIME_DB_ALIGNMENT=NEON_RUNTIME_CONFIRMED \
  npx tsx scripts/backfill/ingest.ts --apply
```

تُخزَّن في شجرة `document_units` بالأنواع القائمة على الفرع:
`INSTRUMENT_OPENING` · `RECITAL` · `INSTRUMENT_CLAUSE` · `SYSTEM_PREAMBLE` ·
`INSTRUMENT_CLOSING` · `SIGNATURE`، ويُحدَّث `completeness`.

**تحقّق:** `SYSTEM_PREAMBLE` واحد · ٥ `RECITAL` على الأقل · ٧ `INSTRUMENT_CLAUSE`
· ومجموع نصّ الوحدات = النصّ الأصلي حرفيًّا (قاعدة عدم السقوط).

### المرحلة (ب) — الآثار النظامية

الجدول الهدف **غير موجود**: المواصفة تطلب `legal_effect` بـ**١٦ نوع أثر**،
والفرع فيه `document_relations` بـ**٥ أنواع فقط**
(`REPLACES`/`REPEALS`/`AMENDS`/`IMPLEMENTS`/`CITES`) — ولا تغطّي «إلغاء جزئي»
ولا «حكم انتقالي» ولا «استثناء نطاق».

**قرار معماريّ مطلوب من المالك:** توسيع `document_relations`، أم إضافة
`legal_effect` كما تنصّ المواصفة؟ **لا تُدخل الآثار قبل حسمه.**

### المرحلة (ج) — حصر الناقص

```bash
npm run audit:numbering   # قراءة فقط
```
ثم أخرج قائمة الأنظمة بلا ديباجة مرتّبة بالأولوية (الأكثر استشهادًا أولًا)،
وابدأ بها خطّ `fetch-boe` بدفعات ≤٢٠.

---

## ٤) ممنوعات

1. **لا تكتب نصّ أي ديباجة بنفسك** — تُستخرج آليًّا من ملف المصدر الرسمي.
   البوابة `scripts/audit/load-official-articles.ts` مُتحقَّقة في الاتجاهين.
2. **لا كتابة إنتاجية بلا موافقة صريحة.**
3. **لا تُدخل الآثار قبل حسم المرحلة (ب).**
4. **لا تُعِد بناء أي فهرس قبل اعتماد Neon.**

---

## ٥) تحذير لازم — تبعيّة حاكمة

**إزاحة ترقيم نظام المعاملات المدنية (المواد ٢٣٧–٧٢١) لم تُصحَّح بعد.**
تأكّد من ذلك حيًّا قبل البدء: اسأل المدونة عن المادة ٤٦٦؛ إن أعادت
«إذا هلك الشيء أو تلف…» فالإزاحة قائمة.

**الأثر:** أيّ `legal_effect` يستهدف مادةً في ذلك النظام برقم ≥٢٣٧ سيرتبط
بالمادة الخطأ. والآثار الثمانية في هذه الحزمة **تستهدف أنظمة أخرى**، فلا
تتأثر — **لكن الدفعات القادمة قد تتأثر**. مرّر كل هدف عبر `number_map` في
`reports/legal-data-audit-2026-09-11/MADANI-RENUM-001.json`.

الإصلاح جاهز: `reports/legal-data-audit-2026-09-11/21-renumber-apply.sql`.

---

## ٦) حدود ما سُلّم — تُذكر صراحةً

1. **ديباجة واحدة فقط.** ملفات `batch{1..5}_preambles.json` (~٧٨ نظامًا)
   **لم تصل** هذه الجلسة — غير موجودة في المستودع ولا على الفرع ولا في Drive.
2. **النصّ غير مُطابَق مع البوابة الرسمية** في جلسة الإعداد (حجب شبكي).
   هو منقول حرفيًّا من ملف موجود في المستودع منذ ما قبل التدقيق، وبصمتاه
   مسجّلتان في `_meta`. **يلزم تأكيد بشريّ.**
3. **بنود المنطوق مُلخَّصة** في `system.mantuq` لأغراض الفهرسة؛ **النصّ الحرفي
   الكامل في `ingest_payload.rawText` وهو المرجع** عند أي تعارض.
4. `~٤٨٤ نظامًا` ما تزال بلا ديباجة.

---

## ٧) صيغة البدء

> اعمل على `hakeem-platform`، الفرع `aman/legal-db-update`. اقرأ
> `PREAMBLES-TASK.md` كاملًا والتزم به.
>
> نفّذ **المرحلة (أ) فقط**: أدخل `data/backfill/text/civil-transactions-law-preamble.json`
> عبر `scripts/backfill/ingest.ts` بعد معاينة، وسلّمني تقرير التحقق
> (عدد الوحدات بأنواعها + إثبات أن مجموع النصّ = الأصل).
>
> **لا تكتب نصّ أي ديباجة بنفسك. لا تُدخل الآثار النظامية — تحتاج قرارًا
> معماريًّا أولًا. توقّف بعد (أ) وانتظر القرار.**
