# التقرير الشامل لفحص منصّة حكيم

> **التاريخ:** ١٤/٠٨/٢٠٢٦م · **النطاق:** كل الخدمات والحسابات والأيقونات والعلاقات وقواعد البيانات والبوّابات والذكاء الاصطناعيّ وصناديق البحث.
> **المنهجيّة:** فحصٌ على مستوى **الكود** (٤ فرق فحص متوازية على HEAD الحاليّ) + **إشارة حيّة** من واجهة `hakeem` MCP ومعاينات Vercel. **حدّ صريح:** لا وصول لقاعدة Neon الحيّة ولا للأسرار في بيئة الفحص، فكل رقم/تغطية تحتاج تشغيلًا حيًّا مُوسَّم **[يحتاج قاعدة حيّة]**. الوسوم: **[حيّ]** قِسته الآن · **[كود]** ثابتٌ في الشيفرة · **[تقرير]** من تدقيقٍ داخليّ.

---

## ٠) لوحة الصحّة (Scorecard)

| المجال | الحالة | الخلاصة بجملة |
|---|:---:|---|
| الذكاء الاصطناعيّ وبوّابته | 🟡 | يعمل (Claude حصريًّا في القاضي)، لكن حصريّة Claude غير مفروضة مركزيًّا، ولا مهلة على أي نداء مزوّد |
| البحث في **المواد** النظاميّة | 🟢 | سليمٌ وممتاز (هجين BM25+دلاليّ) — مؤكَّد حيًّا |
| البحث في **الأحكام** | 🔴 | مكسور: استدعاء ≈صفر، إيجابيّات زائفة (n‑gram)، بلا تطبيع عربيّ — مؤكَّد حيًّا |
| البحث الموسّع (الاسأل) | 🟠 | كان يتوقّف صامتًا؛ **أُصلح في PR #632** (لم يُدمج) |
| قواعد البيانات | 🟡 | Neon هي المرجع؛ لكن يُحتمل غياب جدولَي `embeddings` و`legal_relations` عنها |
| العلاقات (Knowledge Graph) | 🟠 | الكود كامل، لكن البيانات غالبًا فارغة/غائبة في الإنتاج |
| الخدمات والمسارات (158) | 🟢 | غالبيّتها حيّة؛ الصوت والمرفقات‑v2 والحصص مطفأة بالأعلام |
| الحسابات والمصادقة | 🟡 | Clerk حيّ، لكن **فشلٌ مفتوح** إن غابت مفاتيح Clerk في الإنتاج |
| البوّابات: الدفع/الحصص | 🟠 | Moyasar فقط، خاملٌ بلا مفتاح؛ الحصص مطفأة؛ سرّ Webhook «ناعم» |
| الأيقونات والهوية البصريّة | 🟡 | نظام ألوان متماسك، لكن ٣ أنظمة أيقونات متعايشة و«⌕» نصّيّ في أبرز صندوقين |
| التنقّل وRTL | 🟢 | كل الروابط صحيحة، RTL متّسق، لا صفحات ميتة (عدا مكوّن `legal-chat` يتيم) |

---

## ١) الذكاء الاصطناعيّ وبوّابته — 🟡

- **البوّابة** `lib/modules/ai/ai-gateway.ts` + `ai-config.ts`: حلّ التهيئة من قاعدة (مفتاح مُشفَّر AES‑256‑GCM) ← بيئة، مع تسجيل استهلاك الرموز. **[كود]**
- ✅ **حصريّة Claude** مفروضة في `callCentralProvider` (القاضي التفاعليّ): أي مزوّدٍ غير Anthropic ⇒ سقوطٌ حتميّ آمن.
- 🟠 **لكنّها ليست مركزيّة:** `completeWithConfig`/`streamWithConfig` ومسارا `createConsultationDraft`/`original-hakeem` ما زالت تحوي مسارات نداءٍ **لـ OpenAI/Gemini/custom**. مفتاح OpenAI مضبوطٌ في القاعدة يقود التوليد القانونيّ خلافًا للقاعدة المعلنة. **[كود]**
- 🟠 **بلا أي مهلة على نداءات المزوّدين** (لا `AbortController` في كل الملفّ) — هذا **جذر عطل البحث الموسّع** (§البحث)، وخطرٌ نظاميّ.
- 🟠 **نقطة عمياء في القياس:** استهلاك الرموز يُسجَّل لـ Anthropic فقط؛ توليدات OpenAI/Gemini (وGemini هو الاحتياطيّ الشامل) **لا تُحاسَب**.
- ℹ️ OpenAI مستخدمٌ مشروعًا في التضمين (`text-embedding-3-small`) والصوت؛ Gemini في OCR.

## ٢) صناديق البحث والاسترجاع — 🟢 مواد / 🔴 أحكام

**١٣ صندوق بحث** [كود]، أبرزها: الاسأل (`/api/ai/agent-search`، بثّ)، الهجين (`/dashboard/legal-search`)، النواة، القاضي المساعد. 

- 🟢 **المواد النظاميّة:** أكّدتُ حيًّا — «فسخ العقد للغبن» أعاد م.110/111/440/441/475 من نظام المعاملات المدنيّة بنصوصٍ كاملة. **[حيّ]**
- 🔴 **الأحكام:** مكسور — أكّدتُ حيًّا: «غبن»→١٢، «الغبن»→٠، «المدعي»→٠ (في كل حكم!)، «نجش»→٥ **زائفة تطابق «سونجشانج»** (مطابقة تحت‑كلمة)، فلترة السنة تُصفّر النتائج. السبب: فهرسٌ على حقلٍ ضيّق بـ n‑gram بلا تطبيع عربيّ (تقرير `rulings-db-audit-report.md`). **[حيّ]**
- 🟠 **تكرار صناديق:** ثلاثة مداخل «بحث قانونيّ» متنافسة (شريط علويّ + الهجين + نواة‑متقدّم) بلا تمييزٍ للمستخدم.
- 🔴 **كود ميت:** `LegalChatWorkspace` + `/api/legal-chat` غير مُشار إليهما (الصفحة تُحوِّل لـ `ask?mode=chat`).

## ٣) قواعد البيانات والعلاقات — 🟡 / 🟠

- **٥٧ نموذجًا · ١٥ enum · ٢٧ هجرة.** المرجع الحاسم `docs/operations/production-db-source-of-truth.md`: **Neon `neondb` هي قاعدة التشغيل** (489 نظام / 15,902 مادة / 51,105 حكم)؛ Supabase `bnzicgymocelefeqiwig` نسخةٌ قديمة صغيرة (9/1,981/0) تلقّت كتاباتٍ تاريخيّة بالخطأ. **[كود/تقرير]**
- 🔴 **الأخطر:** الوثيقة نفسها تذكر أن جدولَي **`embeddings` و`legal_relations` غير موجودَين على Neon** — فالبحث الدلاليّ والـ Knowledge Graph **فارغان/غائبان وقت التشغيل** رغم اكتمال الكود. **[تقرير — يحتاج قاعدة حيّة للتأكيد]**
- 🟠 **العلاقات (KG):** الاشتقاق حقيقيّ (`relation-derivation.ts`: مادة→حكم، حكم→مبدأ) والأنواع مُستهلَكة لا تجميليّة — لكن البيانات غالبًا غير مبثوثة في الإنتاج.
- 🟠 **فهرسة غير متماثلة:** `search_norm`/tsvector/GIN/trgm على **المواد فقط**؛ الأحكام بلا فهرسٍ نصّيّ (يفسّر §٢).
- 🟠 **جداول بلا مفاتيح أجنبيّة** (polymorphic): `LegalRelation` و`Embedding` بلا FK ⇒ صفوفٌ يتيمة محتملة عند الحذف.
- 🟡 **انحراف مخطّط:** `usage_credit_*` في الهجرات دون نماذج Prisma؛ أعمدة `norm_*` خارج المخطّط — فـ`schema.prisma` ليس خريطةً أمينة لأي قاعدة فيزيائيّة واحدة.
- 🟡 **معرّفات مختلطة:** 49 نموذج cuid مقابل 5 uuid.

## ٤) الخدمات والمسارات — 🟢

**١٥٨ مسار API.** غالبيّتها حيّة (الاسأل، الاستشارة، النواة، القاضي المساعد، المحاكاة، المحادثات، الوظائف، الإدارة). **[كود]**
- **مطفأ بالأعلام في الإنتاج:** الصوت كلّه (503 ما لم يُفعَّل)، المرفقات‑v2 ومعالجة المستندات‑v2، فرض الحصص `USAGE_CREDITS_V2`، بثّ OCR، الاستيراد بالرابط.
- **مفعّل افتراضيًّا:** الوكيل الأصيل، فرض JDS، البحث الدلاليّ (إن وُجد مفتاح)، OCR صفحيّ، OpenSearch (إن وُجد URL).
- 🟠 استخراج نصّ PDF/DOCX في المسار القديم = **TODO غير منجَز** (`attachments/route.ts:237`)؛ الاستخراج الحقيقيّ خلف علم مطفأ.
- 🟠 **إعادة تشغيل الوظائف وهميّة:** `retryJob` يُنشئ وظيفةً «queued» بلا عاملٍ يشغّلها — زرّ الإدارة مُضلِّل.

## ٥) الحسابات والمصادقة والأمان — 🟡

- **Clerk حيّ** + كوكي مالكٍ HMAC احتياطيّ. المسارات المحميّة: `/dashboard`, `/admin`, `/audit-logs`, `/onboarding`. **[كود]**
- 🔴 **فشلٌ مفتوح (الأخطر أمنيًّا):** إن غابت مفاتيح Clerk في الإنتاج و`REQUIRE_AUTH` غير مضبوط، يصير **كل زائرٍ مجهولٍ ضيفًا بصلاحية `SYSTEM_ADMIN`** — ولا حارس يفرض وجود Clerk في الإنتاج. (`session.ts:30‑92,141`). *ملاحظة: علم `DISABLE_AUTH` القديم ميّتٌ الآن؛ هذا هو المكافئ الحيّ.*
- ℹ️ **فجوة منتج لا ثغرة:** **لا `entityType`** (فرد/منشأة — وهو مُميِّزك مقابل قانونية) **ولا `SubscriptionTier`** في المخطّط ولا في التسجيل. التسجيل يُنشئ مستخدمًا في القاعدة عبر Clerk (دور افتراضيّ TRAINEE)، لكن خيار «المحامي الفرد» **غير مبنيّ**.
- 🟢 **الإدارة محميّة:** كل صفحات `/admin` (18) ومساراتها (28) تفرض حارسًا؛ لا صفحة إدارة مكشوفة. (لكن ضيف `SYSTEM_ADMIN` في وضع الفشل المفتوح يصل لبعضها.)
- 🟠 **PDPL:** التنقية قبل إرسال أي شيء لـ Claude **حيّة ومتّسقة** (`sanitizeForModel`) — عدا `callCentralProvider` (تمريرةٌ خام لا تُنقّي ذاتيًّا، تعتمد على المتّصل). و**بيانات المستخدم المُوافَق عليها تُخزَّن نصًّا صريحًا** (`consultation.facts`) بلا تشفير حقليّ.
- 🟡 **سجلّ التدقيق:** واسع (88 موضعًا) لكن **بلا زمن استجابة** وبحقولٍ غير متّسقة.

## ٦) البوّابات: الدفع والحصص وحدّ المعدّل — 🟠

- 🟠 **الدفع: Moyasar فقط** (لا Stripe/Tamara/Tabby). بلا `MOYASAR_SECRET_KEY` يتحوّل الدفع إلى «قريبًا». الخطط: free/pro (149﷼)/team (399﷼) — **لا خطة Enterprise**.
- 🔴 **سرّ Webhook «ناعم»:** إن لم يُضبط `MOYASAR_WEBHOOK_SECRET` يقبل الـ webhook أحداث «paid» **بلا مصادقة** ويفعّل الاشتراك (`webhook/route.ts:42‑58`).
- 🟠 **نظام الحصص/الأرصدة** مبنيٌّ كاملًا لكن **مطفأ** (`USAGE_CREDITS_V2` افتراضيًّا OFF) ⇒ فعليًّا حصّة مجّانيّة (20) ثم نقاط ولاء.
- 🟠 **حدّ المعدّل هجين:** بوّابة API الخارجيّة ذرّيّة على القاعدة (سليمة)، لكن `consumeDirectUrlRateLimit` (الصوت/الاستيراد/الدعم) **في الذاكرة لكل عمليّة** — غير فعّال على عدّة نسخ/serverless.

## ٧) الأيقونات والهوية البصريّة والتنقّل — 🟡 / 🟢

- 🟡 **ثلاثة أنظمة أيقونات متعايشة:** `lucide-react` (المعيار، 53 ملفًّا)، `JaIcon` (خريطة دلاليّة على lucide — جيّد)، و`HakeemIcons` (SVG مخصّص، 4 ملفّات في منصّة الوثائق فقط — يُكرّر مفاهيم lucide). **INCONSISTENT.**
- 🟡 **«⌕» نصّيّ** بدل أيقونة lucide في أبرز صندوقين (الشريط العلويّ + الرئيسة) — تباينٌ بصريّ.
- 🟢 **الهوية اللونيّة متماسكة:** رموز navy/gold/ink في `globals.css`، ونظام تصميم `design-system.tsx` — لكن التبنّي مختلط (رموز سليمة، تنسيق مكوّنات متفرّق).
- 🟢 **التنقّل:** كل الروابط تشير لصفحاتٍ موجودة، لا رابط ميت ولا تكرار (عدا مصفوفتَي تنقّل تحتاجان مزامنة يدويّة). **RTL متّسق** عبر 69 ملفًّا، لا تسريب LTR.

---

## أخطر ١٠ مخاطر (موحّدة، مرتّبة)

| # | الخطورة | الخطر | المجال |
|---|:---:|---|---|
| ١ | 🔴 | **فشلٌ مفتوح للمصادقة** إن غابت مفاتيح Clerk ⇒ زائرٌ = `SYSTEM_ADMIN` | الأمان |
| ٢ | 🔴 | **بحث الأحكام مكسور** (استدعاء ≈صفر + إيجابيّات زائفة) — مؤكَّد حيًّا | البحث |
| ٣ | 🔴 | **`embeddings`/`legal_relations` غالبًا غير منشورَين على Neon** ⇒ الدلاليّ + KG فارغان | القاعدة |
| ٤ | 🔴 | **سرّ Webhook «ناعم»** ⇒ تفعيل اشتراكٍ باحتيالٍ محتمل | الدفع |
| ٥ | 🟠 | **البحث الموسّع كان يتوقّف صامتًا** — أُصلح في PR #632 (لم يُدمج) | البحث/الذكاء |
| ٦ | 🟠 | **تسريب حصريّة Claude** — مسارات OpenAI/Gemini حيّة خارج الحارس المركزيّ | الذكاء |
| ٧ | 🟠 | **بلا مهلة على أي نداء مزوّد** — خطرٌ نظاميّ للتعليق | الذكاء |
| ٨ | 🟠 | **PII نصّيّ عند الموافقة** + تمريرة نموذجٍ خام لا تُنقّي ذاتيًّا | الأمان/PDPL |
| ٩ | 🟠 | **انقسام قاعدتين** (Neon مقابل Supabase) — كتاباتٌ تاريخيّة للقاعدة الخطأ | القاعدة |
| ١٠ | 🟡 | **فجوة منتج:** لا `entityType` (المحامي الفرد) ولا `SubscriptionTier` — غير مبنيّة | الحسابات |

---

## خطة إصلاح مرتّبة

### 🔴 عاجل (أمان/سلامة قبل أي نموّ)
1. **إغلاق الفشل المفتوح:** افرض `REQUIRE_AUTH=1` في الإنتاج، أو اجعل غياب مفاتيح Clerk يُفشِل الإقلاع بدل السقوط لضيف `SYSTEM_ADMIN`.
2. **تشديد سرّ Moyasar Webhook** إلى إلزاميّ (رفض الأحداث غير الموقّعة).
3. **تأكيد وجود جدولَي `embeddings` و`legal_relations` على Neon** بتشغيل `qa:embedding-source` / `audit:db-env` (للقراءة فقط)، ثم بثّهما إن غابا.

### 🟠 قصير المدى (وظائف جوهريّة)
4. **إصلاح بحث الأحكام:** فهرس PostgreSQL Full‑Text (tsvector عربيّ + GIN) على `judgmentText`، أو تفعيل محلّل OpenSearch العربيّ الموجود — يزيل الإيجابيّات الزائفة ويعيد الاستدعاء.
5. **دمج PR #632** (البحث الموسّع) بعد اختبار المعاينة.
6. **فرض حصريّة Claude مركزيًّا** داخل `completeWithConfig` (لا في المتّصلين فقط)، و**إضافة مهلة `AbortSignal`** لكل نداء مزوّد.
7. **تنقية ذاتيّة في `callCentralProvider`** + تشفير حقليّ لـ `consultation.facts` عند الموافقة.
8. **تفعيل مسار الدفع** (مفتاح Moyasar) أو إخفاء الخطط المدفوعة صراحةً حتى الجاهزيّة.

### 🟢 بنيويّ (منتج ونظافة)
9. **بناء `entityType` (فرد/منشأة) + `SubscriptionTier`** في المخطّط والتسجيل — تفعيلٌ لمُميِّزك التنافسيّ.
10. **توحيد الأيقونات** على lucide (استبدال «⌕» و`HakeemIcons`)، **حذف كود `legal-chat` الميت**، **توحيد صناديق البحث** الثلاثة، **عامل طابور حقيقيّ** للوظائف، ونقل حدّ المعدّل في‑الذاكرة إلى مخزنٍ مشترك.

---

### خاتمة الصدق المنهجيّ
هذا فحصٌ على مستوى الكود + إشاراتٍ حيّة محدودة. الأرقام والتغطية الفعليّة على الإنتاج (تغطية المتجهات، عدد الأحكام الآن، وجود الجداول على Neon) تُحسَم فقط بتشغيلٍ للقراءة على `NEON_DATABASE_URL` — وهو ما لا أملكه في بيئة الفحص. ما وسمته **[حيّ]** قِسته بنفسي؛ وما وسمته **[تقرير]/[يحتاج قاعدة حيّة]** يحتاج تأكيدًا حيًّا قبل بناء قرارٍ نهائيّ عليه.
