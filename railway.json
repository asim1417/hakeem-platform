{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "context": "tools/arabic-doc-tool"
  },
  "deploy": {
    "startCommand": "uvicorn tool_app:app --host 0.0.0.0 --port 8080",
    "restartPolicyMaxRetries": 5
  }
}
