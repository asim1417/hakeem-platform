# تقرير إصلاح توقّف «البحث الموسّع التفصيلي» — منصّة حكيم

> الفرع: `aman/extended-search-fix` · لم يُدمج في main. · النطاق: مسار الويب فقط (لا يُمَسّ منطق البحث المشترك مع `/mcp`).

## ١) نقطة الانقطاع — مثبَتة بالدليل

**المسار:** صندوق البحث → `ask()` في `components/ask/HakeemAskWorkspace.tsx:922` → `POST /api/ai/agent-search` (بثّ NDJSON) → في وضع «اسأل» يدخل **الوكيل الأصيل** `runHakeemAgent` (`route.ts:541`)، وزرّ «دراسة موسّعة» (`detailed`) تلميحٌ صريحٌ يدفع Claude لمزيد من جولات الأدوات (`route.ts:540`).

**العلّة الجذريّة (فرضية ف١ + ف٣ مندمجتان):** الخطوة الختامية (حلقة الوكيل) تُجري نداءات نموذجٍ **بلا مهلة**:
- `lib/modules/hakeem-agent/anthropic.ts` — `fetch("…/v1/messages")` **بلا `AbortSignal`** وغير متدفّق (كل نداء يحجب حتى اكتمال التوليد).
- الحلقة `for turn 0..maxTurns` حتى **٢٠ جولة** (`flag.ts` — `hakeemAgentMaxToolTurns`)، **بلا سقفٍ زمنيّ كلّيّ**.

فإذا تعثّر اتّصال المزوّد (تعليقٌ بلا رفض) أو تراكمت الجولات، يتجاوز الزمن الكلّيّ `maxDuration` (`route.ts:66` = 300s، **ويُقصَر إلى 60s على خطة Vercel Hobby**)، فتَقتُل المنصّة الدالّة **قبل** إرسال `result`/`done`. والقتل ليس استثناءً JS، فلا يلتقطه `try/catch` (`route.ts:986`) ولا يُنفَّذ `finally{finish()}` ولا يُحدَّث الـ job إلى `done` — فلا نتيجة، ولا خطأ، ولا استئناف: **توقّفٌ صامت** يطابق العَرَض حرفيًّا.

**العرَض على الواجهة (ف٢/ف٤ كأثرٍ للجذر):** عند انتهاء البثّ دون حدثٍ ختاميّ، كانت الحلقة تنهي وتضبط `streaming:false` **بلا جواب وبلا خطأ** (`HakeemAskWorkspace.tsx:1019`) — تحميلٌ يختفي دون نتيجة ولا رسالة.

**استُبعد:** الوسيط (Clerk) — لا يُخزّن استجابات البثّ، والمسار يضبط `X-Accel-Buffering:no`. والمنطق المشترك مع `/mcp` سليمٌ (مؤكَّد) لأنّه نداءٌ واحدٌ بلا حلقة نموذجٍ خادميّة — وهنا مكمن الاختلاف.

## ٢) ما غُيِّر ولماذا (٤ تعديلات، دفاعٌ متعدّد الطبقات)

| # | الملف | التغيير | الأثر |
|---|---|---|---|
| ١ | `lib/modules/hakeem-agent/anthropic.ts` | مهلة لكلّ نداء نموذج عبر `AbortSignal.timeout` (افتراضي 75s، `HAKEEM_AGENT_CALL_TIMEOUT_MS`) | اتّصالٌ متعثّر يُجهَض ويعود خطأً تتعامل معه الحلقة، بدل تعليقٍ أبديّ |
| ٢ | `lib/modules/hakeem-agent/runtime.ts` | ميزانيّة زمنيّة كلّيّة للحلقة (افتراضي 210s، `HAKEEM_AGENT_BUDGET_MS`)؛ عند تجاوزها يُجرَّد النداء التالي من الأدوات فيُنتِج النموذج **إجابةً نهائيّة** ممّا جُمع (يسلك مسار «لا أدوات» بحارس الإسناد نفسه) | تسليمٌ رشيقٌ مضمونٌ ضمن `maxDuration`، بلا جولةٍ إضافيّة تتجاوز الحدّ |
| ٣ | `app/api/ai/agent-search/route.ts` | مهلة `withTimeout` (90s) حول خطوتَي الصياغة `synthesizeWithMode` و`createConsultationDraft` | لو تعثّرت الصياغة في المسار القياسيّ، يسقط لعرض المواد المُتحقَّقة + `done` بدل الصمت |
| ٤ | `components/ask/HakeemAskWorkspace.tsx` | تتبّع وصول حدثٍ ختاميّ (`sawTerminal`)؛ لو انتهى البثّ دونه ولا جواب ⇒ رسالة خطأٍ عربيّة صريحة + إطفاء التحميل (مع إحالةٍ للاستئناف إن وُجد `jobId`) | لا توقّف صامت أبدًا: المستخدم يرى نتيجة أو رسالة واضحة |

**ضوابط مُحترَمة:** لم يُمَسّ منطق البحث/الاسترجاع المشترك مع `/mcp`. لا تغيير على schema ولا على متغيّرات البيئة (المهل قابلة للضبط بمتغيّرات اختياريّة بقيمٍ افتراضيّة آمنة). الهوية البصريّة كما هي (رسالة خطأٍ ضمن نمط `error` القائم للدور).

## ٣) لماذا لا يكفي رفع المهلة وحده (وفق الأمر)

رفع `maxDuration` لا يحسم شيئًا لو ظلّ النداء بلا مهلةٍ داخليّة (قد يتجاوز أي سقف). لذلك خفّضنا زمن الخطوة الختامية فعليًّا: مهلة لكلّ نداء + ميزانيّة كلّيّة تُنهي الحلقة بإجابةٍ رشيقة قبل الحدّ، لا مجرّد رفع السقف.

> **توصية تشغيليّة (تحتاج تأكيدك — لا تُنفَّذ آليًّا):** تأكّد أن خطة Vercel **Pro** ليُحترَم `maxDuration=300`؛ فعلى **Hobby** يُقصَر فعليًّا إلى 60s فتظهر المشكلة رغم الإصلاح على الاستعلامات الأثقل. الميزانيّة الافتراضيّة (210s) مضبوطةٌ لخطة Pro؛ على Hobby اضبط `HAKEEM_AGENT_BUDGET_MS=45000` و`HAKEEM_AGENT_CALL_TIMEOUT_MS=30000`.

## ٤) التحقّق

- **بناء الإنتاج `next build`:** ✅ ناجح.
- **`tsc --noEmit`:** ✅ لا أخطاء في الملفّات المعدّلة ولا في المشروع (بعد `db:generate`).
- **تتبّع المسار منطقيًّا:**
  - استعلامٌ قصير: يكتمل في جولةٍ أو جولتين قبل أي مهلة — سلوكٌ كما كان.
  - استعلامٌ ثقيل («فسخ العقد» موسّع): إن قاربت الحلقة الميزانيّة، تُنتِج إجابةً نهائيّةً ممّا جُمع + `result`/`done` — لا قتلَ صامت.
  - فشلٌ مفتعَل (تعليق/مهلة النموذج): المهلة تُجهض النداء → خطأٌ مُعالَج → سقوطٌ للمسار القياسيّ أو رسالة خطأٍ صريحة على الواجهة.
- **حدود التحقّق (يُصرَّح بها):** التحقّق الحيّ end-to-end (استعلام حيّ على الإنتاج) يتطلّب نشر معاينة Vercel بالأسرار (مفتاح Anthropic + قاعدة + مصادقة) غير المتوفّرة في بيئة الإصلاح. **يُوصى باختبار معاينة الـ PR** بالاستعلامات الثلاثة قبل الدمج.

## ٥) خلاصة

العطل لم يكن في البحث، بل في **غياب أي سقفٍ زمنيّ على نداءات النموذج في الخطوة الختامية لمسار الويب**، فتُقتل الدالّة قبل التسليم. الإصلاح يضمن — بأربع طبقات — أن كلّ طلبٍ ينتهي إمّا بنتيجةٍ معروضة أو برسالةٍ عربيّة واضحة، ضمن حدّ المنصّة، دون مساسٍ بمنطق البحث المشترك.
