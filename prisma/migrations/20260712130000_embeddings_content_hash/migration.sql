-- ═══════════════════════════════════════════════════════════════════════════
-- [EMB — كشف التقادم] إضافة عمود content_hash لجدول embeddings.
-- الغرض: تمكين إعادة التضمين التزايُدية عند تغيّر نصّ المصدر (أو النموذج) بدل
-- التخطّي الأعمى لكل ما «له متجه». عمود «model» موجود مسبقًا ويغطّي تغيّر النموذج.
-- idempotent، إضافي، لا يحذف بيانات. القيمة تُملأ عبر backfill-embeddings.ts.
-- ⚠️ تُطبَّق عبر workflow مُقفَل على Neon (لا تُشغَّل تلقائيًا عند النشر).
-- ═══════════════════════════════════════════════════════════════════════════

ALTER TABLE "embeddings" ADD COLUMN IF NOT EXISTS "content_hash" text;
