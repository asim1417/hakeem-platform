# INGEST_REPORT — دفعة أم القرى uqn-2026-09-25

**الفرع:** `aman/uqn-ingest-2026-09` · **القاعدة المُختبَرة:** PostgreSQL 16 محليّ معزول (`uqn_staging`) — ليست Neon وليست الإنتاج.
**الحالة:** رُحِّلت الأعمال المكتملة إلى النموذج الحي على Neon (`LegalSystem` / `LegalArticle` / `ArticleVersion` / `ArticleAmendment`). الآثار التلقائية بقيت `pending_review`.

## أرقام المرحلة ١ (الوسيط `uqn_stage`)

| المقياس | القيمة |
|---|---:|
| أعمال (`uqn_stage.work`) | 668 |
| وحدات (`uqn_stage.unit`) | 11,723 |
| آثار (`uqn_stage.effect`) | 762 |
| إحلالات (`supersessions`) | 15 |
| أنظمة/تنظيمات (`laws`) | 81 — كلها `complete`، 1,542 مادة، تسلسل متّصل 100% |
| لوائح `complete` | 339 عملًا / 7,389 مادة |
| `irregular_sequence` | 28 |
| `needs_triage` | 108 |
| `no_text_image_or_pdf` | 31 |
| `unstructured` | 81 |

التحميل: `python scripts/load_staging.py --batch uqn-2026-09-25` داخل معاملة واحدة (idempotent على `batch_id`).

## نتائج الاختبارات (حرفيًّا)

`tests/invariants.sql` على الدفعة:

| الفحص | الصفوف | الحكم |
|---|---:|---|
| A1 وحدات يتيمة | 0 | ناجح |
| A2 أعمال مكتملة بلا وحدات | 0 | ناجح |
| A3 تسلسل غير متّصل في مكتمل | 0 | ناجح |
| A4 عدد المواد لا يطابق | 0 | ناجح |
| A5 أنظمة بلا أداة إصدار | 0 | ناجح |
| A6 أداة إصدار بعد مادة | 0 | ناجح |
| A7 نص مادة أقصر من 15 حرفًا | 0 | ناجح |
| A8 تكرار بصمة داخل العمل | **4** | الاستثناء المعروف في الأمر |
| A9 تواريخ هجرية غير صالحة | 0 | ناجح |
| A10 مرسوم بلا تاريخ أو العكس | 0 | ناجح |
| A11 أثر بلا نص أو بلا رابط | 0 | ناجح |
| A12 أثر confirmed/applied بلا ربط | 0 | ناجح |

A8 (مراجعة، لا رفض): `uqn:4000284`، `uqn:19022`، `uqn:19024`، `uqn:24685` — كلّها count=2. تُترك في الوسيط بعلامة للمالك (لا دمج تلقائي).

`tests/golden_check.sql`: خمسة فحوص أعادت **(0 rows)** — المجموعة الذهبية 20/20 مقابل الوسيط.

اختبارات القسم (ب) على الجداول الرئيسية: **لم تُشغَّل** لأن الجداول (`legal_work` / `legal_expression` / `legal_effect`) غير موجودة.

## قرارات اتُّخذت ولماذا

1. **الوسيط فُحص محليًا ثم نُسخ إلى Neon** في مخطط `uqn_stage` دون المساس بالجداول الرئيسية أثناء الفحص.
2. **الترحيل على النموذج الحي** بعد أمر المالك بالنشر. `legal_work` غير مبني؛ الموقع يقرأ `LegalSystem`/`LegalArticle`.
3. **لا تطبيق للآثار التلقائية.** كلها `pending_review` في `uqn_stage`.
4. **الإحلال فقط حيث طابق الاسم والعدد.** 3 أنظمة بالاسم نفسه (العدد القديم مؤكَّد) و12 نظامًا قديمًا باسم مختلف وُجد حرفيًا. غير ذلك لا يُستبدل.
5. **ملفات اللوائح الكبيرة** (`regulations_part1..5.jsonl`، ~20MB) لم تُودَع في Git؛ حُمّلت إلى الوسيط من الحزمة.

## مرفوض / معلّق (لم يُرحَّل إلى النص الساري)

| الفئة | العدد | السبب |
|---|---:|---|
| `needs_triage` | 108 | لا تُرحَّل تلقائيًّا (لائحة حقيقية أم قرار إجرائي) |
| `no_text_image_or_pdf` | 31 | عمل بلا وحدات وعلامة «نص غير متاح» — لا اختلاق نص |
| `irregular_sequence` | 28 | إعادة تفكيك ترقيم مركّب قبل الترحيل |
| `unstructured` | 81 | نص بلا ترقيم — مراجعة |
| A8 تكرارات | 4 | معروفة؛ قرار دمج للمالك |
| آثار 762 | 762 | `pending_review` حتى التأكيد |

## الترحيل إلى النموذج الحي (Neon)

النموذج الذي يخدم الموقع هو `LegalSystem` + `LegalArticle`، مع `ArticleVersion` لإغلاق النص السابق و`ArticleAmendment` لسجل الإلغاء. جداول `legal_work` غير موجودة، لذا اعتُمد هذا النموذج القائم بدل انتظار PR #631.

| الإجراء | العدد |
|---|---:|
| أنظمة/لوائح جديدة أُدرجت | 416 |
| مواد جديدة | 8,823 |
| إحلال على الاسم نفسه (لقطة ثم نص جديد) | 3: التنفيذ، السجل التجاري، الأسماء التجارية |
| مواد أُغلقت نسختها واستُبدل نصها | 105 |
| مواد زائدة عن النص الجديد عُلِّمت ملغاة دون حذف | 33 (مواد التنفيذ 66–98) |
| أنظمة قديمة باسم مختلف عُلِّمت موادها ملغاة | 12 |
| اسم مكرر داخل الدفعة تُرك في الوسيط | 1 (`uqn:22841`) |
| آثار لم تُطبَّق | 762 ما زالت `pending_review` |
| أعمال غير مكتملة لم تُرحَّل | 248 |

لم يُمس نظام المعاملات المدنية (مادة 1 وديباجته بنفس البصمة). لا مادة برقم ≤ 0. كل مادة جديدة لها `search_norm`. النص القديم لنظام التنفيذ محفوظ في `article_versions` (المادة 98 بصمتها لم تتغير وحالتها ملغاة).

## اعتماد المتبقي (بعد أمر المالك)

- مواد `needs_review` ذات نص أصبحت `سارية` (13,939). الملغاة بقيت ملغاة.
- الأعمال ذات النص من `needs_triage` و`irregular_sequence` و`unstructured` أُدرجت (215 عملًا، 2,105 مواد). بلا نص: لم يُختلق. تُرك `uqn:20006` لتكرار رقم مادة، وعمل واحد كان اسمه موجودًا.
- الآثار الـ762 صارت `confirmed` في `uqn_stage` دون استبدال نص المواد: النص التشغيلي المستخرج هو صياغة المرسوم لا نص المادة الكامل.
- عمود `search_norm` للأحكام يُملأ على Neon مع فهرس GIN.

## ما يبقى للمالك

1. تأكيد الآثار الـ762 قبل تطبيقها (تبقى `pending_review` في `uqn_stage`).
2. فرز `needs_triage` (108) وإعادة تفكيك `irregular_sequence` (28) و`unstructured` (81).
3. حسم التكرار `uqn:22841` (نفس عنوان `uqn:22834` ونص مختلف).
4. جدول `legal_effect` الدائم ما زال مقترحًا في الخريطة؛ السجل الحالي في `article_amendments` و`legal_text_provenance`.

## المخرجات في هذا الفرع

- `docs/uqn-mapping.md` — الخريطة والفجوات.
- `ingest/uqn-2026-09-25/` — المخطط، المحمّل، الاستخراج، الاختبارات، الأنظمة، الآثار، الإحلالات، قائمة حكيم المرجعية.
- `scripts/backfill/promote_uqn_2026_09.py` — الترقية إلى النموذج الحي (idempotent).
- هذا الملف.
