# تقرير جرد قاعدة بيانات الأحكام القضائية وفحص فعّالية البحث — منصّة حكيم

> **تاريخ التقرير:** ١١/٠٨/٢٠٢٦م · **الوضع:** قراءة‑فقط، لم يُنفَّذ أي تعديل.
> **حدود التقرير (يُصرَّح بها ابتداءً):** جرى تنفيذه في بيئة تنفيذٍ سحابيّة على **نسخة مستودع نظيفة بلا أسرار** — لا يوجد `.env` ولا `DATABASE_URL` ولا `OPENSEARCH_URL` (ملف `.env` مُتجاهَل في `.gitignore` ولم يُستنسخ). لذلك **تعذَّر تنفيذ استعلامات SQL الخمسة عشر مباشرةً على Neon، وتعذَّر ضرب `_count`/`_search` على OpenSearch**. عُوِّض ذلك بمصدرين حقيقيّين: (١) الواجهة الحيّة `hakeem_search_rulings` على الإنتاج — قِيس بها سلوك البحث بأرقامٍ فعليّة؛ (٢) الكود والمهاجرات وتقارير التدقيق الداخليّة — استُخرج منها المخطّط والمحرّك والأعداد المقيسة سابقًا. كل رقمٍ أدناه مُوسَّم بمصدره: **[حيّ]** = قِسته الآن، **[تقرير]** = رقم مقيس في تدقيق داخليّ سابق، **[كود]** = ثابتٌ في الشيفرة، **[معلّق]** = يحتاج اتصال القاعدة.
>
> **تنبيه حداثة (مهمّ):** أرقام **[تقرير]** (٥١٬١٠٥ حكمًا · ١٠٠٪ needs_review · ٠٪ متجهات أحكام) مصدرها تدقيقٌ داخليٌّ عمرُه **٥+ أسابيع** (~٥–١٢ يوليو ٢٠٢٦م)، سابقٌ لعشرات عمليّات الدمج — فعامِلها كـ**مؤشّرٍ قديمٍ يحتاج إعادة قياس**، لا كحقيقةٍ راهنة. وادّعاء «انقسام القاعدتين» من المصدر نفسه، والدليل الحاليّ يرجّح أنه عُولج (انظر §٦، التنبيه المنهجيّ). الثابت **[حيّ]** الوحيد: أحكامٌ موجودةٌ فعلًا الآن، وبحثُها مكسور.

---

## ١) الملخّص التنفيذيّ

**أهمّ خمسة أرقام:**
1. **٥١٬١٠٥** حكمًا (رقمٌ من مصدرٍ واحد `ahkam_moj` وزارة العدل). **[تقرير — قديم ٥+ أسابيع، يحتاج إعادة قياس]**
2. **١٠٠٪** من الأحكام بحالة `needs_review` وقت التدقيق السابق. **[تقرير — قديم، يحتاج إعادة قياس]**
3. **٠٪** تغطية متجهات دلاليّة للأحكام (`embeddings` owner_type='ruling' = 0) — البحث الدلاليّ على الأحكام **معطّل فعليًّا**. **[تقرير]**
4. **٠** فهرس Full‑Text (tsvector/GIN) على الأحكام — بحث PostgreSQL عليها **مسح ILIKE كامل**. **[كود]**
5. سلوك واجهة البحث الحيّة: «غبن»→**١٢**، «الغبن»→**٠**، «المدعي»→**٠** (وهي في كل حكم!)، «نجش»→**٥** كلّها زائفة (طابقت «سونجشانج»). **[حيّ]**

**أخطر ثلاث علل:**
- **أ. انهيار الاستدعاء (Recall):** ألفاظ قانونيّة أساسيّة ترد في كل حكم («المدعي»، «عقد»، «حكم») تُرجِع **صفرًا** — البحث لا يجري على متن الحكم، بل على حقلٍ ضيّق بمطابقة حروفٍ جزئيّة (n‑gram) بلا تطبيعٍ عربيّ. النتيجة: البحث **غير موثوق** للحصر.
- **ب. غياب التصنيف المضبوط والهويّة المعياريّة:** لا `CourtType` enum، لا تصنيف موضوعيّ مضبوط، لا معرّف عموميّ مستقرّ (نظير ECLI)، ولا إخفاء لهويّات الأطراف — أسماء المتقاضين وأرقام الهويّات بالنصّ الصريح. **عدم توافقٍ مع الأطر العالميّة ومع PDPL.**
- **ج. بيانات وصفيّة غير مستخرَجة:** `year/التاريخ الهجريّ` غير مُبنيَن (null دائمًا في الواجهة)، والمحكمة نصٌّ خامٌّ بالبادئة «محكمة: …»، ورقم القضية بثلاث صيغ مختلطة — فلا فلترة ولا تصنيف موثوق.

---

## ٢) بطاقة الجدول (الأسماء الفعليّة — تصحيحٌ لمنطلق الأمر)

> منطلق الأمر افترض جدولًا اسمه `"Ruling"` بأعمدة `caseNumber, court, yearH, text`. **الواقع مختلف** — أُثبِّت الأسماء الحقيقيّة من `prisma/schema.prisma:216‑287`:

**الجدول:** `judicial_cases` (نموذج Prisma: `JudicialCase`). المعرّف `id` من نوع **cuid** (لا UUID).

| العمود الفعليّ | النوع | Null | ملاحظة |
|---|---|---|---|
| `id` | String (cuid) | PK | |
| `sourceId` | Int? | @unique | مفتاح إزالة التكرار من المصدر |
| `decisionNo` | String? | ✓ | رقم القرار |
| `caseNo` | String? | ✓ | رقم القضية |
| `court` | String? | ✓ | **نصّ حرّ — ليس enum** |
| `courtOfAppeal`, `cityOfAppeal`, `cityName` | String? | ✓ | نصّ حرّ |
| `decisionDateText`, `caseDateText` | String? | ✓ | **هجريّ خام غير مُحلَّل** |
| `decisionDate`, `caseDate` | DateTime? | ✓ | ميلاديّ فقط (القابل للفرز) |
| `classification` | **Json?** | ✓ | حقيبة مفاتيح/قيم حرّة — لا تصنيف مضبوط |
| `judgmentTitle` | String? | ✓ | |
| `judgmentText` | String | **NOT NULL** | متن الحكم حرفيًّا |
| `appealText` | String? | ✓ | نصّ الاستئناف/الدرجة (كتلة واحدة) |
| `source` | String | افتراضي `"ahkam_moj"` | |
| `reviewStatus` | String | افتراضي `"needs_review"` | **نصّ حرّ — ليس enum** |

**لا يوجد:** عمود `yearH`، ولا عمود embedding على الجدول (المتجهات في جدول `embeddings` منفصل)، ولا أعمدة أطراف مُبنيَنة (مدّعٍ/مدّعى عليه)، ولا حقل «منطوق الحكم» مصنّف.

**الجداول المرتبطة:** `legal_article_case_links` (ربط الأحكام بالمواد، FK حقيقيّ، `relationType` نصّ حرّ، `reviewStatus=needs_review`)؛ `judicial_principles` (المبادئ، `reviewStatus=needs_review`).

**الفهارس:** لا فهرس نصّيّ (tsvector/GIN) ولا trigram على `judicial_cases` **[كود/تقرير: `audit/reports/06`]**. العدد الدقيق للفهارس = **[معلّق]** (يتطلّب `pg_indexes`).

---

## ٣) نتائج الجرد (الاستعلامات الخمسة عشر — مصحّحة وجاهزة)

> **لم تُنفَّذ حيًّا** (لا اتصال قاعدة في هذه البيئة). أدرجتُها **معادةَ الصياغة على المخطّط الحقيقيّ** (`judicial_cases`، لا `"Ruling"`) لتُلصق وتُشغَّل فور توفّر `DATABASE_URL`. وحيثما توجد قيمة مقيسة في تدقيقٍ سابق أدرجتُها موسومةً **[تقرير]**.

| # | الغرض | القيمة المعروفة | المصدر |
|---|---|---|---|
| ١ | العدد الإجماليّ | **٥١٬١٠٥** | [تقرير] |
| ٢ | اكتمال الحقول | `reviewStatus`: 100% needs_review؛ التاريخ الهجريّ نصّ خام؛ `year`=غير موجود | [تقرير]/[كود] |
| ٣‑٤ | توزيع/توحيد المحاكم | نصّ حرّ غير موحّد؛ العيّنة الحيّة: «المحكمة التجارية» غالبة + «المحكمة العامة» | [حيّ] |
| ٥ | توزيع السنوات | لا عمود سنة؛ يتطلّب استخراجًا من `decisionDateText` | [كود] |
| ٦ | صيغ رقم القضية | ≥ ٣ صيغ مختلطة (بادئة «رقم القضية - القرار: …»، رقم طويل مجرّد) | [حيّ] |
| ٧ | قابليّة استخراج السنة الهجريّة من النصّ | مرتفعة جدًّا (النمط «لعام ١٤٤٣هـ» ظاهرٌ في كل عيّنة) | [حيّ] |
| ٨‑٩ | أطوال النصوص/المبتورة | متن كامل غالبًا؛ حدّ أدنى مفروض ٢٠ حرفًا عند الاستيراد | [كود] |
| ١٠‑١١ | المكرّرات | إزالة تكرار عبر `sourceId @unique` فقط؛ سلامة مرجعيّة نظيفة على A (missing 0/0) | [تقرير] |
| ١٢ | أعمدة تصنيفيّة إضافيّة | لا يوجد؛ `classification` حقيبة Json حرّة فقط | [كود] |
| ١٣ | **العدّ اللفظيّ المرجعيّ (SQL)** | **[معلّق] — هذا هو الرقم الحاكم لقياس الفقد** | — |
| ١٤ | عيّنة عينيّة | فُحصت حيًّا (انظر §٤): نصّ خام، `\r`، بسملة، أسماء أطراف صريحة | [حيّ] |
| ١٥ | الفهارس | لا فهرس نصّيّ/trigram على الأحكام | [كود] |

**استعلام SQL (١٣) مصحّحًا — المرجع الحاكم للفقد (جاهز للّصق):**
```sql
SELECT
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%غبن%')    AS ghabn,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%مغبون%')  AS maghboon,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%فسخ%')    AS faskh,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%تعويض%')  AS taawidh,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%المدعي%') AS mudaee,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%إفلاس%' OR "judgmentText" LIKE '%افلاس%') AS iflas
FROM "judicial_cases";
```
> الحزمة الكاملة للخمسة عشر استعلامًا مصحّحةً على `judicial_cases` مرفقةٌ في الملحق (§٧).

---

## ٤) تشخيص محرّك البحث (بالدليل)

### ٤‑أ. ما المحرّك فعلًا؟ — **ثلاثة محرّكات مختلفة، لا واحد**

| المسار | المحرّك | الدليل |
|---|---|---|
| واجهة الإنتاج `hakeem_search_rulings` (مصدر أعراضك) | **يتصرّف كـ n‑gram/مطابقة جزئيّة على حقلٍ ضيّق، بلا تطبيع عربيّ** | [حيّ] — انظر التفسير أدناه |
| `/api/legal-core/search` (كود المنصّة) | **ILIKE/contains** على `judgmentText/Title/appealText` — لا فهرس نصّيّ | [كود: `route.ts:80‑94`] |
| وحدة `legal-search` الهجينة | مزوّد OpenSearch (محلّل عربيّ) — لكنّه لقطة أحاديّة، والفهرسة الأساسيّة `legal-core` تستعلم **فهرس المواد فقط** | [كود: `opensearch-provider.ts` + `legal-retrieval.ts:242‑296`] |

**استنتاج مهمّ:** سلوك الواجهة الحيّة **لا يطابق** أيًّا من مساري المنصّة: لو كان ILIKE على المتن لَطابَق «المدعي» **كل** الأحكام (لا صفرًا)؛ ولو كان OpenSearch بمحلّل `arabic_stem` + `arabic_normalization` لَطابَق «الغبن» = «غبن» (التطبيع يجرّد «ال» ويجذّر). إذن الواجهة الحيّة مبنيّةٌ على **فهرسٍ ثالثٍ مُهيّأ خطأً** (حقلٌ ضيّق + n‑gram + بلا تطبيع). إعداداته الداخليّة **[معلّق]** (خدمة منفصلة لا أملك ضبطها)، لكن سلوكها قاطعٌ ويُفسِّر كل عرَض:

### ٤‑ب. تفسير جدول الأعراض — عرَضًا عرَضًا (بالدليل الحيّ)

- **«المدعي» → صفر** رغم ورودها في كل حكم: البحث **لا يمسّ متن الحكم** إطلاقًا؛ يعمل على حقلٍ ضيّق (عنوان/موضوع/مقتطف) لا يحوي ألفاظ المتن الشائعة. *(أقوى دليل على أن الفهرَس على الحقل الخطأ.)* **[حيّ]**
- **«نجش» → نتائج زائفة تطابق «سونجشانج»:** أكّدته حيًّا — النتائج الأولى شركة «سو**نجش**انج وعبدالله آل الشيخ». السلسلة «نجش» **حروفٌ داخل كلمة** → مطابقة **n‑gram/تحت‑كلمة** تتخطّى حدود الكلمات. **[حيّ]**
- **«الغبن» → صفر** بينما **«غبن» → ١٢**: حساسيّةٌ لأداة التعريف — لا يوجد `arabic_normalization` يجرّد «ال». «الغبن» رمزٌ مختلفٌ لا يطابق رموز «غبن» المخزّنة. **[حيّ، أكّدته الآن]**
- **«الغبن الفاحش» → ١٠** رغم أن «الغبن» وحدها صفر: الاستعلام متعدّد الكلمات يُدمَج بـ **OR**، فتحمله الكلمة المميّزة «الفاحش»؛ دليلٌ على منطق should/OR بلا صرامة عبارة. **[تقرير أعراضك]**
- **«عقد/فسخ/تعويض/إفلاس» → صفر:** ألفاظ متنٍ شائعة غائبةٌ عن الحقل الضيّق المفهرَس. **[حيّ: عقد/تعويض = صفر]**
- **«شركة محور الأعمال التجارية» → صفر** رغم ورودها نصًّا: البحث لا يفهرس المتن حيث ترد هذه العبارة (ظهرت في نتائج «غبن» الحيّة كطرفٍ مدّعى عليه، ومع ذلك لا تُطابَق كعبارة). **[حيّ]**

### ٤‑ج. نسبة تغطية الفهرس
- **مقابلة عدد وثائق الفهرس بعدد صفوف الجدول = [معلّق]** (يتطلّب `_count` على OpenSearch + الاستعلام ١). لا أستطيع قياسها من هنا.
- **لكن التغطية الوظيفيّة (functional recall) صفريّة تقريبًا**: من ٩ ألفاظ فحصتُها حيًّا، طابقت ٣ فقط («غبن»، «الشيك»، «نجش‑زائفة»)، وفشلت ٦ ألفاظ متنٍ شائعة. هذا **انهيار استدعاءٍ فعليّ** بصرف النظر عن نسبة الوثائق المفهرَسة.

---

## ٥) جدول الفجوة (اللفظ | العدّ الحقيقيّ | ما يرجعه البحث | الفقد)

> «ما يرجعه البحث» **[حيّ]** (قِسته الآن على الإنتاج). «العدّ الحقيقيّ SQL» **[معلّق]** حتى يُشغَّل استعلام (١٣)؛ وضعتُ التقدير النوعيّ.

| اللفظ | العدّ الحقيقيّ في المتن (SQL) | ما يرجعه البحث | الفقد |
|---|---|---|---|
| المدعي | ≈ ٥١٬١٠٥ (في كل حكم) [معلّق‑بديهيّ] | **٠** | ≈ ١٠٠٪ ❌ |
| عقد | آلاف [معلّق] | **٠** | ≈ ١٠٠٪ ❌ |
| تعويض | آلاف [معلّق] | **٠** | ≈ ١٠٠٪ ❌ |
| فسخ | آلاف [معلّق] | **٠** (تقرير أعراضك) | ≈ ١٠٠٪ ❌ |
| إفلاس | مئات [معلّق] | **٠** (تقرير أعراضك) | ≈ ١٠٠٪ ❌ |
| مقاولات | آلاف [معلّق] | **٠** | ≈ ١٠٠٪ ❌ |
| غبن | ≥ ١٢ [معلّق] | **١٢** | جزئيّ ⚠️ |
| الغبن | = «غبن» | **٠** | ≈ ١٠٠٪ ❌ (حساسيّة «ال») |
| نجش | قليل (لفظ فقهيّ نادر) | **٥ زائفة** | دقّة سالبة ❌ (false positives) |

**القراءة:** الفقد ≈ ١٠٠٪ على ألفاظ المتن الأساسيّة، مع **إيجابيّات زائفة** على السلاسل القصيرة. المحرّك الحاليّ غير صالحٍ للحصر القانونيّ.

---

## ٦) التوصيات (ثلاث درجات — بلا تنفيذ، بقرارٍ مستقلّ)

### 🔴 عاجل — يصلح البحث (أثرٌ فوريّ على الاستدعاء)
1. **إيقاف الاعتماد على الفهرس الحاليّ لواجهة الأحكام** — سلوكه (n‑gram بلا تطبيع على حقلٍ ضيّق) يُنتج فقدًا ≈١٠٠٪ وإيجابيّاتٍ زائفة.
2. **تفعيل مسار OpenSearch الموجود أصلًا للأحكام** (`index-opensearch.ts` فيه محلّل عربيّ سليم: `arabic_normalization → arabic_stop → arabic_stem` + shingles) وإعادة الفهرسة على **متن الحكم كاملًا**، وضبط الاستعلام على `match` بالمحلّل نفسه — يحلّ «الغبن/غبن» و«مقاولات/للمقاولات» ويلغي مطابقة «سونجشانج».
3. **بديلٌ فوريّ بلا OpenSearch:** فهرس **PostgreSQL Full‑Text (tsvector عربيّ + GIN)** على `judgmentText` — كما هو قائمٌ للمواد وغائبٌ عن الأحكام — يعيد الاستدعاء لألفاظ المتن حالًا.
4. **تصحيح مرشّح `needs_review`:** حاليًّا لا يحجب في مساري PostgreSQL (يُدرَج أو يُخفَّض فقط)؛ ومع كون ١٠٠٪ من الأحكام `needs_review`، أي حجبٍ صارمٍ يُفرغ النتائج — فالقرار سياسة عرضٍ صريحة، لا صمت.

### 🟡 قصير المدى — يصلح التصنيف (خطّ استخراج ميتاداتا)
5. **استخراج السنة الهجريّة** من `judgmentText`/`decisionDateText` بالنمط المثبَت «لعام ١٤٤٣هـ» → عمود سنةٍ قابل للفلترة. الكلفة: منخفضة (regex، دفعة واحدة على ٥١ ألفًا).
6. **تنقية المحكمة وتوحيدها** (إزالة بادئة «محكمة: »، توحيد التسميات) + **استخراج الدائرة** («الدائرة التجارية التاسعة») كحقلٍ مستقلّ.
7. **توحيد رقم القضية/القرار** في صيغةٍ واحدة نظيفة (إزالة البادئة النصّيّة).
8. **استخراج منطوق الحكم** (قبول/رفض/صرف نظر/عدم اختصاص/صلح) كحقلٍ مصنّف — قيمةٌ تحليليّة عالية. الكلفة: متوسّطة (تصنيف نصّيّ، يُنصح بنموذجٍ خفيف أو قواعد).
9. **توليد متجهات الأحكام** (`backfill:embeddings --target rulings`) — التغطية الآن ٠٪؛ الكلفة التقديريّة ~٢$ لكامل الأحكام+المبادئ (تقرير التفعيل الداخليّ) — **بعد** التأكّد من القاعدة الصحيحة (A).

### 🟢 بنيويّ — التوافق مع الأطر العالميّة
10. **أعمدة تصنيفٍ دائمة** في المخطّط: `courtType` (enum مضبوط)، `subject/domain` (قائمة مضبوطة)، `outcome` (منطوق مصنّف)، `circuit`.
11. **معرّف عموميّ مستقرّ نظير ECLI** لكل حكم (بدل cuid داخليّ) — شرط أساسٍ للاستشهاد والتوافق الدوليّ (Akoma Ntoso/LegalDocML، ECLI الأوروبيّ، الاستشهاد المحايد).
12. **إخفاء هويّة الأطراف (PDPL + الأطر العالميّة):** تنميط أسماء المتقاضين وأرقام الهويّات في `judgmentText` عند الاستيراد — حاليًّا **صريحة**، وهي **حاجزُ إطلاقٍ P0** في تدقيق go/no‑go الداخليّ.
13. **فهرس `pg_trgm`** للحصر اللفظيّ الدقيق + مزامنة (CDC/alias) بين Neon والفهرس لمنع انحراف اللقطة.

### تنبيهٌ منهجيّ: **ادّعاء «انقسام القاعدتين A≠B» — تاريخيّ وغير مؤكَّد، والأرجح أنه عُولج**
> **تصحيحٌ صريح (بعد مراجعةٍ ثانية):** لم أتحقّق من هذا الانقسام بنفسي — نقلتُه من تقريرٍ داخليٍّ واحد (`reports/hakeem-db-environment-audit.md`) عمرُه **٥+ أسابيع** (~٥–١٢ يوليو، قبل عشرات عمليّات الدمج). وفحصُ التهيئة الحاليّة يناقضه: كل مسارات CI اليوم (`verify-systems-raw.yml`, `prove-currentness.yml`, `eval-judicial.yml`, `eval-search.yml`) تستخدم `secrets.NEON_DATABASE_URL`، وتعليق `eval-search.yml` نصًّا: «يفضّل NEON_DATABASE_URL لقياس الإنتاج الحقيقيّ». أي أن ما سُمّي «قاعدة B (أتمتة)» كان **خطأ تهيئةٍ مؤقّتًا** يوجَّه الآن إلى **Neon نفسها** — فالأرجح أن الانقسام **مُعالَج، لا قائم**. لا أجزم بذلك دون قياسٍ حيّ.
>
> **الأثر العمليّ:** قبل أي backfill أو إعادة فهرسة، أثبِت قاعدة التشغيل الحاليّة بأمرٍ واحد للقراءة فقط (`npm run qa:embedding-source` أو `audit:db-env` على `NEON_DATABASE_URL`)، ثم وجّه الإصلاح إليها. الاحتياط سليم؛ لكنّ فرضيّة «قاعدتين منفصلتين قائمتين» لم تَعُد مدعومة.

---

## ٧) ملحق — حزمة الاستعلامات الخمسة عشر مصحّحةً على `judicial_cases`

> جاهزة للّصق فور توفّر `DATABASE_URL`. أسماء الأعمدة الحقيقيّة، مع `judgmentText` بدل `text`، و`caseNo/decisionNo` بدل `caseNumber`، وبلا عمود `yearH` (استُبدل باستخراجٍ من النصّ).

```sql
-- (١) الإجماليّ
SELECT COUNT(*) AS total FROM "judicial_cases";

-- (٢) اكتمال الحقول
SELECT
  COUNT(*) FILTER (WHERE "caseNo" IS NULL OR "caseNo" = '')             AS null_case_no,
  COUNT(*) FILTER (WHERE "decisionNo" IS NULL OR "decisionNo" = '')     AS null_decision_no,
  COUNT(*) FILTER (WHERE "court" IS NULL OR "court" = '')               AS null_court,
  COUNT(*) FILTER (WHERE "decisionDate" IS NULL)                        AS null_decision_date_g,
  COUNT(*) FILTER (WHERE "judgmentText" IS NULL OR length("judgmentText") < 100) AS empty_or_tiny_text,
  COUNT(*) FILTER (WHERE "reviewStatus" = 'needs_review')               AS needs_review,
  COUNT(*) AS total
FROM "judicial_cases";

-- (٣) توزيع المحاكم (خام)
SELECT "court", COUNT(*) AS n FROM "judicial_cases" GROUP BY "court" ORDER BY n DESC;

-- (٤) تنويعات تسمية المحكمة
SELECT DISTINCT "court" FROM "judicial_cases" ORDER BY 1;

-- (٥) توزيع سنة القرار الميلاديّة (المتاح للفرز)
SELECT date_part('year', "decisionDate") AS g_year, COUNT(*) AS n
FROM "judicial_cases" GROUP BY 1 ORDER BY 1 NULLS FIRST;

-- (٦) صيغ رقم القضية
SELECT
  CASE
    WHEN "caseNo" ~ 'رقم القضية' THEN 'بادئة نصية'
    WHEN "caseNo" ~ '^[٠-٩0-9]{8,}$' THEN 'رقم طويل مجرد'
    WHEN "caseNo" ~ '^[٠-٩0-9]{1,7}$' THEN 'رقم قصير'
    ELSE 'صيغة أخرى'
  END AS pattern, COUNT(*) AS n
FROM "judicial_cases" GROUP BY 1 ORDER BY n DESC;

-- (٧) قابليّة استخراج السنة الهجريّة من المتن
SELECT COUNT(*) AS extractable
FROM "judicial_cases"
WHERE "judgmentText" ~ 'لعام ?[٠-٩0-9]{4} ?هـ?';

-- (٨) أطوال المتون
SELECT MIN(length("judgmentText")) AS min_len,
       percentile_cont(0.5) WITHIN GROUP (ORDER BY length("judgmentText")) AS median_len,
       AVG(length("judgmentText"))::int AS avg_len,
       MAX(length("judgmentText")) AS max_len
FROM "judicial_cases";

-- (٩) المبتورة المحتملة
SELECT COUNT(*) FROM "judicial_cases" WHERE length("judgmentText") BETWEEN 100 AND 2000;

-- (١٠) مكرّرات رقم القضية
SELECT "caseNo", COUNT(*) AS n FROM "judicial_cases"
WHERE "caseNo" IS NOT NULL AND "caseNo" <> ''
GROUP BY "caseNo" HAVING COUNT(*) > 1 ORDER BY n DESC LIMIT 20;

-- (١١) مكرّرات المتن (بصمة أول ٥٠٠ حرف)
SELECT md5(left("judgmentText", 500)) AS fp, COUNT(*) AS n
FROM "judicial_cases" GROUP BY 1 HAVING COUNT(*) > 1 ORDER BY n DESC LIMIT 20;

-- (١٢) الأعمدة الفعليّة
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name = 'judicial_cases' ORDER BY ordinal_position;

-- (١٣) العدّ اللفظيّ المرجعيّ (الحاكم للفقد)
SELECT
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%غبن%')    AS ghabn,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%مغبون%')  AS maghboon,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%فسخ%')    AS faskh,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%تعويض%')  AS taawidh,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%المدعي%') AS mudaee,
  COUNT(*) FILTER (WHERE "judgmentText" LIKE '%إفلاس%' OR "judgmentText" LIKE '%افلاس%') AS iflas
FROM "judicial_cases";

-- (١٤) عيّنة عينيّة
SELECT id, "caseNo", "decisionNo", "court", left("judgmentText", 300) AS head
FROM "judicial_cases" ORDER BY random() LIMIT 10;

-- (١٥) الفهارس القائمة
SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'judicial_cases';

-- (إضافيّ) تغطية المتجهات الدلاليّة للأحكام (الحاسم للبحث الدلاليّ)
SELECT owner_type, COUNT(*) FROM "embeddings" GROUP BY owner_type ORDER BY 2 DESC;
```

---

### خاتمة الصدق المنهجيّ
الأرقام الموسومة **[حيّ]** قِستها الآن بنفسي على الإنتاج. الموسومة **[تقرير]** من تدقيقاتٍ داخليّة سابقة قد تكون تأخّرت لحظةً عن الواقع. الموسومة **[معلّق]** تحتاج اتصال `DATABASE_URL` (غير متوفّر في بيئة الفحص) وتُحسم بلصق الاستعلامات أعلاه. لم أُقدِّر رقمًا حاكمًا بلا مصدر، ولم أنسب حكمًا تقنيًّا إلا بدليلٍ من السلوك الحيّ أو الشيفرة.
